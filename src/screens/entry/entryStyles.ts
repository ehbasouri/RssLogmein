import {StyleSheet} from 'react-native';

export const entryStyles = StyleSheet.create({
  entryItemContainer: {
    borderColor: '#C1C1C1',
    borderWidth: 0.5,
    padding: 20,
    marginTop: 20,
    backgroundColor: '#fff',
  },
  link: {
    color: 'blue',
  },
});
