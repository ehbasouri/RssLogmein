import {StyleSheet} from 'react-native';

export const feedStyles = StyleSheet.create({
  feedItemContainer: {
    borderColor: '#C1C1C1',
    borderWidth: 0.5,
    padding: 20,
    marginTop: 20,
    backgroundColor: '#fff',
  },
  loadingContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  }
});
